
spring.datasource.url=jdbc:postgresql://localhost:5432/trainingdb
spring.datasource.username=swarnim
spring.datasource.password=swarnim
spring.jpa.hibernate.ddl-auto=create-drop
server.port=8080


spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=trainingdb
spring.data.mongodb.repositories.enabled=true

spring:
kafka:
consumer:
group-id: tpd-loggers
auto-offset-reset: earliest
# change this property if you are using your own
# Kafka cluster or your Docker IP is different
bootstrap-servers: localhost:9092
tpd:
topic-name: advice-topic
messages-per-request: 10

